{
  "name": "horoscope-studio",
  "version": "0.1.0",
  "private": true,
  "main": "electron/main.js",
  "scripts": {
    "api": "py -3 -m uvicorn server.app:app --host 127.0.0.1 --port 8787",
    "api:unix": "python3 -m uvicorn server.app:app --host 127.0.0.1 --port 8787",
    "electron": "electron .",
    "dev": "electron ."
  },
  "devDependencies": {
    "electron": "^34.2.0"
  }
}
